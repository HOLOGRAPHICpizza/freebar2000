package org.peak15.freebar2000.common;

import java.awt.event.ActionListener;

/**
 * Implemented by actions that can be performed on MusicNodes.
 * These actions should be globally registered somewhere, so that nodes know all actions available to them.
 */
public interface MusicNodeAction extends ActionListener {
	//
}
